- hosts: all
  become: true
  tasks:
  - name: "create new deployment"
    command: kubectl apply -f /home/ubuntu/Deployment.yml
  - name: "create new sercices"
    command: kubectl apply -f /home/buntu/Service.yml
    
